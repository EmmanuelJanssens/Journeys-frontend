import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "com.journeys.app",
    appName: "journeys-app",
    webDir: "dist",
    bundledWebRuntime: false
};

export default config;
